import numpy as np
from test_functions import *
from utilities import *
import codecs
# convert from base 2 -> base 10
def decode(pop):
    #np.packbits(pop)

    return 0

# normalize range of xy to fit search space
def normalize(xy, xy_range):

    return 0

def get_fitness(xy, f):

    return 0

@Counter.count
def ga(f, pop_size=100, genome_size = 11, tol=1e-7, max_iter=1000, verbose=False, plotting=False):

    # Initialize population
    #xy_range = get_range(f)
    #pop = np.matrix(np.random.randint(2, size=[pop_size,genome_size]))
    #print(pop)
    #print(decode(pop))

    return 0